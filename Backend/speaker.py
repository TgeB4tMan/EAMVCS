def get_speaker_embedding(audio_path):
    print(f"Extracting speaker embedding from {audio_path}")

    # Dummy embedding for now
    embedding = [0.0] * 256

    print("Speaker embedding shape:", len(embedding))
    return embedding
