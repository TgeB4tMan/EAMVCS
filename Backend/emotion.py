def get_emotion_embedding(audio_path):
    print(f"Extracting emotion embedding from {audio_path}")

    # Dummy emotion embedding
    embedding = [0.0] * 128

    print("Emotion embedding shape:", len(embedding))
    return embedding
